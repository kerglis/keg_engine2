%h1= t("devise.passwords.change_password")

= simple_form_for @user, url: user_pswd_path do |f|
  = f.input :password
  = f.input :password_confirmation

  .form-actions
    = f.submit t("devise.passwords.change_password"), class: "btn btn-primary"